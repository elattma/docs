---
title: "Retrieve Context"
api: "GET /context"
---

## Query Parameters

<ParamField query="query" type="string" required>
  A description of the desired context

  <Tip>Read [Formulating a Query](/api-reference/endpoints/context/formulating-a-query) to learn best practice with the `query` parameter.</Tip>

</ParamField>
<ParamField query="max_tokens" type="integer" default="2000">
  The maximum number of tokens to retrieve
</ParamField>
<ParamField query="use_sample_data" type="boolean" default="False">
  Determines whether to retrieve context from the [Test Dataset](/documentation/data/test-dataset)
</ParamField>
<ParamField query="next_token" type="string">
  See [Pagination](/api-reference/overview/pagination) for reference
</ParamField>
## Response
<ResponseField name="contexts" type="Context[]" required>
  A list of `Context` objects that represent the context most relevant to the `query`. See [The Context Object](/api-reference/endpoints/context/the-context-object) for reference
</ResponseField>
<ResponseField name="next_token" type="string">
  See [Pagination](/api-reference/overview/pagination) for reference
</ResponseField>
<RequestExample>

```bash cURL
curl --request GET --get \
     --url https://api.mimo.team/context \
     --header 'x-api-key: AUTH_VALUE' \
     --data query='last email from Troy Wilkerson' \
     --data max_tokens=4000
```

```python Python
import requests

url = 'https://api.mimo.team/context'
headers = {'x-api-key': AUTH_VALUE}
params = {
  'query': 'subject of last email from Troy Wilkerson',
  'max_tokens': 4000
}
response = requests.get(url, headers=headers, params=params)

```

</RequestExample>
<ResponseExample>
```json Sample Response
{
  "contexts": [
    {
      "text": "Hello, world",
      "score": 1.0,
      "source": {
        "integration": {
          "name": "google_mail",
          "category": "email"
        },
        "page": {
          "id": "1234567890",
          "link": "https://mail.google.com/mail/u/0/#inbox/1234567890",
          "type": "email"
        }
      }
    }
  ]
}
````

</ResponseExample>
